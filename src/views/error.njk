{% extends 'layouts.njk' %}

{% block content %}
  <h1 class="govuk-heading-xl">{{ heading }}</h1>

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <p>{{ message }}</p>
    </div>
  </div>
{% endblock %}
